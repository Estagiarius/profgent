# Este arquivo __init__.py vazio serve para que o Python reconheça o
# diretório 'views' como um pacote, permitindo importações estruturadas
# a partir dele (ex: from app.ui.views import DashboardView).
