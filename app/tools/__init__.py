# Este arquivo __init__.py está vazio, mas ele é necessário para que o Python
# trate o diretório 'tools' como um pacote. Isso permite que módulos de dentro
# deste diretório (como database_tools.py) sejam importados em outras partes
# da aplicação usando a sintaxe de pacote (ex: from app.tools import ...).
